/**
 * Brand assets bundled (and hashed) by Vite from the repo's `images/` dir.
 * Import from here instead of reaching into `images/` directly.
 *
 * WebP, sized to about twice their largest on-screen width: the PNG masters
 * were 3-5x larger than any viewport ever renders them, and the wordmark
 * alone cost 1.9MB on a page every guest opens on mobile data.
 */

import logoVertical from '../images/logo-vertical.webp'
import logo from '../images/logo.webp'
import siriguela from '../images/siriguela.webp'

export { logo, logoVertical, siriguela }
